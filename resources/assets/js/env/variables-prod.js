'use strict';

(function (window) {
    window.env = window.env || {};
    window.env.apiUrl = 'https://www.webatspeed.eu';
    window.env.baseUrl = '/';
    window.env.enableDebug = false;
}(this));
